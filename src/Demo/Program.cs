using Demo;
using SDL3;

/*if (SDL.GetSystemRAM() > 1024)
{
    SDL.ShowSimpleMessageBox(SDL.MessageBoxFlags.Information, "Preload?",
        "You have enough system memory to be able to pre-load this demo. Would you like to?", 0);
}*/

DemoApp.Run();